Четвертое задание: **Создать третьего пользователя С и попробовать повторить действия из задания 3 от его имени**. Этот пользователь не будет иметь своей схемы, и его права будут ограничены.

### PostgreSQL

1. **Подключись к PostgreSQL от имени суперпользователя А**:
   ```bash
   psql -U postgres
   ```

2. **Создай третьего пользователя С**:
   ```sql
   CREATE USER user_c WITH PASSWORD 'password_c';
   ```

3. **Попробуй изменить данные в таблице `students` от имени пользователя С**:

   Сначала подключись как пользователь С:
   ```bash
   psql -U user_c
   ```

   Попробуй выполнить изменение данных:
   ```sql
   UPDATE user_b.students SET age = 23 WHERE name = 'John Doe';
   ```

4. **Попробуй изменить структуру таблицы `students` от имени пользователя С**:
   ```sql
   ALTER TABLE user_b.students ADD COLUMN phone_number VARCHAR(15);
   ```

5. **Ожидаемый результат**:
   - Скорее всего, ты столкнешься с ошибками из-за отсутствия необходимых прав у пользователя С.

### Oracle

1. **Подключись к Oracle от имени суперпользователя А**:
   ```bash
   sqlplus sys as sysdba
   ```

2. **Создай третьего пользователя С**:
   ```sql
   CREATE USER user_c IDENTIFIED BY password_c;
   ```

3. **Попробуй изменить данные в таблице `students` от имени пользователя С**:

   Сначала подключись как пользователь С:
   ```bash
   sqlplus user_c/password_c
   ```

   Попробуй выполнить изменение данных:
   ```sql
   UPDATE user_b.students SET age = 23 WHERE name = 'John Doe';
   ```

4. **Попробуй изменить структуру таблицы `students` от имени пользователя С**:
   ```sql
   ALTER TABLE user_b.students ADD (phone_number VARCHAR2(15));
   ```

5. **Ожидаемый результат**:
   - Также вероятно, что у пользователя С не будет прав на выполнение этих операций, и ты столкнешься с ошибками.

После выполнения этого задания и проверки ошибок, мы перейдём к пятому заданию, которое будет связано с настройкой прав с помощью команды GRANT.
