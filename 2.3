Третье задание: **Попробовать что-то изменить (операторы DML и DDL) в созданных таблицах от лица первого пользователя (суперпользователя А)**. Это даст возможность проверить, какие операции доступны суперпользователю и что нужно изменить.

### PostgreSQL

1. **Подключись к PostgreSQL от имени суперпользователя А**:
   ```bash
   psql -U postgres
   ```

2. **Попробуй изменить данные в таблице `students`** (оператор DML):
   ```sql
   UPDATE user_b.students SET age = 22 WHERE name = 'John Doe';
   ```

3. **Попробуй изменить структуру таблицы `students`** (оператор DDL):
   ```sql
   ALTER TABLE user_b.students ADD COLUMN email VARCHAR(100);
   ```

4. **Проверь изменения**:
   ```sql
   SELECT * FROM user_b.students;
   ```

### Oracle

1. **Подключись к Oracle от имени суперпользователя А**:
   ```bash
   sqlplus sys as sysdba
   ```

2. **Попробуй изменить данные в таблице `students`** (оператор DML):
   ```sql
   UPDATE user_b.students SET age = 22 WHERE name = 'John Doe';
   ```

3. **Попробуй изменить структуру таблицы `students`** (оператор DDL):
   ```sql
   ALTER TABLE user_b.students ADD (email VARCHAR2(100));
   ```

4. **Проверь изменения**:
   ```sql
   SELECT * FROM user_b.students;
   ```

### Возможные результаты:
- Если суперпользователь А имеет полный доступ, то изменения пройдут без проблем.
- Если что-то не удается сделать (например, изменение структуры таблицы), это значит, что требуется дополнительная настройка прав, что будет частью следующего задания.
ю.
