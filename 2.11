### Одиннадцатое задание: **Нарисовать диаграмму взаимодействия ролей**.

Для этого задания тебе нужно создать диаграмму, которая покажет, как взаимодействуют роли в системе. Мы можем использовать информацию о ролях, которые ты создал в предыдущих заданиях (например, `read_only_role`, `data_entry_role`, `manager_role`), и показать, какие права каждая роль предоставляет, и какие пользователи связаны с этими ролями.

#### Пример взаимодействия ролей:

1. **read_only_role**:
   - Права: только чтение данных из таблиц.
   - Пользователи: `user_c`.

2. **data_entry_role**:
   - Права: вставка данных в таблицы.
   - Пользователи: `user_d`.

3. **manager_role**:
   - Права: унаследованные права от `data_entry_role`.
   - Пользователи: `user_d`.

#### Элементы диаграммы:
- **Роли**: это узлы на диаграмме (например, `read_only_role`, `data_entry_role`, `manager_role`).
- **Права**: указываются между ролями и таблицами (например, `SELECT`, `INSERT`).
- **Пользователи**: указываются в связке с ролями, которые они имеют (например, `user_c` для `read_only_role`).

#### Пример схемы:
```
user_c → read_only_role → SELECT ON students
user_d → data_entry_role → INSERT ON students
user_d → manager_role → INHERITS INSERT FROM data_entry_role
```

Ты можешь использовать любой инструмент для построения диаграмм (например, ручное рисование, или программы вроде Draw.io, Microsoft Visio), чтобы изобразить эти связи. 

После того как нарисуешь диаграмму, можно будет обсудить детали или улучшения, если они необходимы.
